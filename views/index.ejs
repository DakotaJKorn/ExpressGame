<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        #container{
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            width: 90vw;
            margin-left: 5vw;
            background-color: #fdffff;
        }
        #loginBox{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 400px;
            width: 500px;
            background-color:cadetblue;
            border-radius: 20px;
        }
        #formContent{
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .formText{
            font-size: large;
            margin-bottom: 3px;
            font-weight: bold;
            cursor:default;
        }
        .formInput{
            border: none;
            border-radius: 3px;
            height: 30px;
            cursor:text;
        }
        .formInput:focus{
            border: none;
        }
        #passwordInput{
            margin-bottom: 10px;
        }

        #submitButton{
            height:30px;
            width: 80px;
            background-color:rgb(164, 189, 189);
            border-radius: 10px;
            color: white;
            border: 1px solid black;
        }
        #submitButton:hover{
            cursor: pointer;
            background-color: rgb(181, 206, 206);
        }
        #errorMessage{
            color: rgb(199, 38, 38);
        }
    </style>
</head>
<body>
    <div id="container">
        <div id="loginBox">
            <form action="/signup" method="POST">
                <div id="formContent">
                    <p class="formText">Username</p>
                    <input class="formInput" type="text" name="username" placeholder="Username">
                    <p class="formText">Password</p>
                    <input class="formInput" id="passwordInput" type="password" name="password" placeholder="Password">
                    
                    <input id="submitButton" type="submit" value="Log In">
                    <% if(error == "invalid_user"){ %>
                        <p id="errorMessage">Username/Password incorrect</p>
                        <% } %>
                </div>
            </form>
        </div>

    </div>

</body>
</html>